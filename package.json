{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "setup": "npm i && lerna bootstrap && lerna run build",
    "start": "lerna run start --stream"
  }
}
