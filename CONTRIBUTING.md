# Contributing

First, thanks to all future contributors 🙏

## Getting started

The easiest way is to use docker and the docker-compose config that we provide.

```bash
docker-compose -f docker-compose.dev.yml up
```
